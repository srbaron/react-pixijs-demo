export interface Position {
  x: number;
  y: number;
}

export interface Canvas {
  width: number;
  height: number;
}

export interface GameProperties {
  zoomScale: number;
  speedScale: number;
  volume: number;
}
